---
# ------------------------------------------------------------------
# Apache installation - only needs to run once. On redeploy runs,
# skip this whole block with --skip-tags apache (dnf/firewalld/service
# tasks are idempotent regardless, but skipping avoids the extra
# package-manager and firewall checks on every redeploy).
# ------------------------------------------------------------------
- name: Install and start Apache
  become: true
  tags:
    - apache
    - install
  block:
    - name: Install Apache (httpd)
      ansible.builtin.dnf:
        name: "{{ apache_package }}"
        state: present

    - name: Ensure firewalld is running
      ansible.builtin.systemd:
        name: firewalld
        state: started
        enabled: true

    - name: Open firewall for HTTP and HTTPS
      ansible.posix.firewalld:
        service: "{{ item }}"
        permanent: true
        immediate: true
        state: enabled
      loop:
        - http
        - https

    - name: Enable and start Apache service
      ansible.builtin.systemd:
        name: "{{ apache_service }}"
        state: started
        enabled: true

# ------------------------------------------------------------------
# Site deployment - runs every time, including redeploys where the
# apache block above is skipped via --skip-tags apache.
# ------------------------------------------------------------------
- name: Deploy website content
  become: true
  tags:
    - deploy
  block:
    - name: Ensure document root exists
      ansible.builtin.file:
        path: "{{ site_doc_root_linux }}"
        state: directory
        owner: "{{ site_owner }}"
        group: "{{ site_group }}"
        mode: "0755"

    - name: Sync website content to document root
      ansible.posix.synchronize:
        src: "{{ site_source_dir_linux }}"
        dest: "{{ site_doc_root_linux }}/"
        delete: true
        recursive: true
      notify: Reload apache

    - name: Set ownership and permissions on deployed content
      ansible.builtin.file:
        path: "{{ site_doc_root_linux }}"
        state: directory
        recurse: true
        owner: "{{ site_owner }}"
        group: "{{ site_group }}"

    - name: Restore SELinux context on document root
      ansible.builtin.command: restorecon -Rv {{ site_doc_root_linux }}
      register: restorecon_result
      changed_when: "'Relabeled' in restorecon_result.stdout"
